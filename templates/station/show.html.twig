{% extends 'frontendindex.html.twig' %}

{% block title %}{{app.user.name}}'s Profile{% endblock %}

{% block body %}


  <!-- Start single page header -->
  <section id="single-page-header">
    <div class="overlay">
      <div class="container">
        <div class="row">
          <div class="col-md-6 col-sm-6 col-xs-12">
            <div class="single-page-header-left">
              <h2>Blog Single</h2>
              <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry</p>
            </div>
          </div>
          <div class="col-md-6 col-sm-6 col-xs-12">
            <div class="single-page-header-right">
              <ol class="breadcrumb">
                <li><a href="#">Home</a></li>
                <li class="active">Blog Single</li>
              </ol>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <!-- End single page header -->

  
  <section id="blog-archive">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="blog-archive-area">

  {% include "user/usersidebar.html.twig" %}
    <h1>User</h1>

              <div class="col-md-8">
                <div class="blog-archive-left">
     <h1>Station index</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ station.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ station.title }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ station.keywords }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ station.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ station.image }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ station.address }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ station.city }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ station.country }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ station.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ station.createdAt ? station.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ station.updatedAt ? station.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ station.detail }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ station.userid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_station_index') }}">back to list</a>

    <a href="{{ path('user_station_edit', {'id': station.id}) }}">edit</a>

    {{ include('station/_delete_form.html.twig') }}
     </div>
        </div>
      </div>
    </div>
  </section>
{% endblock %}
